<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }
      header {
        width: 100%;
        height: 9.5vh;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        padding: 0 5%;
        border-bottom: 0.5px solid grey;
      }
      #logo {
        width: 170px;
        height: 100%;
      }
      nav > ul {
        width: 300px;
        display: flex;
        list-style: none;
        justify-content: space-evenly;
        font-weight: 400;
      }
      h3 {
        text-align: center;
      }
      .container {
        width: 90%;
        height: 55vh;
        margin: 0 auto;
        display: flex;
        padding-top: 15px;
      }
      #result-image {
        width: 420px;
        height: 325px;
      }
      .Main-info {
        display: flex;
        flex-direction: column;
        margin-left: 70px;
      }
      #name {
        font-size: 1.2rem;
        word-spacing: 2px;
        height: 40px;
        margin-top: 5px;
      }
      #description {
        width: 450px;
        height: 300px;
        word-spacing: 2px;
        font-family: sans-serif;
      }
      .price-section {
        width: 90%;
        height: 30vh;
        margin: 40px auto;
        display: flex;
        flex-direction: column;
      }

      .urlAndprice {
        width: 95%;
        height: 50px;
        align-items: center;
        border: 1px solid grey;
        display: flex;
        justify-content: space-between;
        margin: 5px auto;
        padding: 0 40px;
        background-color: white;
        border-radius: 7px;
        margin-bottom: 5px;
        padding-right: 45px;
        box-shadow: 0px 0px 6px 0px rgba(0,0,0,0.34);
      }
    </style>
  </head>
  <body>
    <header>
      <img id="logo" src="/images/logoNew.png" />
      <nav>
        <ul>
          <li>About us</li>
          <li>Guide</li>
          <li>EX</li>
        </ul>
      </nav>
    </header>

    <main>
     
      <section class="top-container">
        <div class="container">
          <img
            id="result-img"
            src="<%= results[0].image %>"
            alt="Image of the chosen bike"
          />
          <div class="Main-info">
             <h2> <div id="brand"><%= results[0].brand %></div></h2>
            <div id="name"><%= results[0].name %> </div>
            <div style="margin-bottom: 5px;">Compare price from <span>£<%= results[0].price%></span></div>
            <div id="description" style="overflow: hidden; text"><%= results[0].description %></div>
          </div>
        </div>
      </section>
    
      <section class="price-section">
        <% resultComparison.forEach((data)=>{ %>
        <div class="urlAndprice">
          <div><a href="<%= data.urls %>" style="text-decoration: none; color:rgb(62, 105, 148)"><%=data.name%> <%=data.brand%></a></div>
          <div>£<%= data.prices %></div>
        </div>
        <% });%>
        <!-- Add other URL and price sections as needed -->
      </section>
    </main>
  </body>
</html>
